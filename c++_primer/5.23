#include <iostream>
#include <cstring>
#include <cstdlib>
#include <ctime>
#include <algorithm>
#include <vector>
int main() {
    using namespace std;

    int val1 = 0, val2 = 0;
    cout << "Please input two value: ";
    cin >> val1 >> val2;
    if(val2 == 0)
    {
        cout << "Error,Divisor can not be 0!" << endl;
        return -1;
    }
    cout << "The result is: " << val1 / val2 << endl;
    return 0;
}
