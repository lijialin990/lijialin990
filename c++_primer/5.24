#include <iostream>
#include <cstring>
#include <cstdlib>
#include <ctime>
#include <algorithm>
#include <vector>
int main() {
    using namespace std;

    double val1 = 0, val2 = 0;
    cout << "Please input two value: ";
    cin >> val1 >> val2;
    if(val2 == 0)
    {
        throw runtime_error("Divisor cannot be 0! ");
    }
    cout << "The result is: " << val1 / val2 << endl;
    return 0;
}
